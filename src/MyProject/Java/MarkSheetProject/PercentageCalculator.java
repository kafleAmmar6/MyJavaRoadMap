package MyProject.Java.MarkSheetProject;

public class PercentageCalculator {
    public static float calculatePercentage(int obtained, int totalFullMarks) {
        return (obtained * 100f) / totalFullMarks;
    }
}